x = float(input("what's x? "))
y = float(input("what's y? "))
z = round(x+y) #round fucntion will round the floting value. We can also spacify the number of floting points we want to round. 
print(f"{z:,}")
